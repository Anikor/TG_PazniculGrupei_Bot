'use strict';window.addEventListener('pageshow',function(e){var n=(performance.getEntriesByType&&performance.getEntriesByType('navigation')[0])||null;if(e.persisted||(n&&n.type==='back_forward'))location.reload();});function onReady(fn){if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',fn,{once:true});}else{fn();}}(function(){window.nav=function(off){const url=new URL(location.href);url.searchParams.set('offset',String(off));url.searchParams.delete('when');location.href=url.toString();};})();(function(){try{const tg=window.Telegram&&window.Telegram.WebApp;if(tg&&typeof tg.expand==='function')tg.expand();}catch{}})();(function(){try{const html=document.documentElement;html.classList.add('js-ready');const saved=localStorage.getItem('theme')||'light';html.classList.toggle('dark-theme',saved==='dark');}catch{}})();onReady(function(){const root=document.documentElement;const toggle=document.getElementById('theme-toggle');const label=document.getElementById('theme-label');if(!toggle||!label)return;const saved=localStorage.getItem('theme')||'light';toggle.checked=saved==='dark';label.textContent=saved==='dark'?'Dark':'Light';root.classList.toggle('dark-theme',saved==='dark');toggle.addEventListener('change',()=>{const isDark=!!toggle.checked;root.classList.toggle('dark-theme',isDark);label.textContent=isDark?'Dark':'Light';localStorage.setItem('theme',isDark?'dark':'light');document.cookie='theme='+(isDark?'dark':'light')+';path=/;max-age=31536000';});});onReady(function(){const tableToggle=document.getElementById('table-toggle');const tableLabel=document.getElementById('table-label');if(!tableToggle)return;const ANIM_MS=260;function applyLayout(mode){document.body.classList.toggle('layout-big',mode==='big');localStorage.setItem('tableLayout',mode);document.cookie='tableLayout='+encodeURIComponent(mode)+';path=/;max-age=31536000';}function setUI(mode){tableToggle.checked=mode==='big';if(tableLabel)tableLabel.textContent=mode==='big'?'Big':'Compact';}const initial=document.body.classList.contains('layout-big')?'big':'small';setUI(initial);tableToggle.addEventListener('change',e=>{const mode=e.target.checked?'big':'small';setUI(mode);setTimeout(()=>{applyLayout(mode);},ANIM_MS);});});onReady(function(){if(!document.getElementById('tg-bootstrap')){try{sessionStorage.removeItem('tgBootTries');}catch{}return;}const tg=window.Telegram&&window.Telegram.WebApp;try{tg&&tg.expand&&tg.expand();}catch{}const initData=tg&&tg.initData;const fail=msg=>{const p=document.createElement('p');p.style.color='red';p.textContent=msg;document.body.innerHTML='';document.body.appendChild(p);};if(!initData){fail('Please open this app from Telegram.');return;}let tries=0;try{tries=parseInt(sessionStorage.getItem('tgBootTries')||'0',10)||0;}catch{}if(tries>=3){try{sessionStorage.removeItem('tgBootTries');}catch{}fail('Could not start a session. Fully close the Mini App and open it again.');return;}try{sessionStorage.setItem('tgBootTries',String(tries+1));}catch{}fetch(location.href,{method:'POST',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify({init_data:initData})}).then(async r=>{let j=null;try{j=await r.json();}catch{}if(r.ok&&j&&j.success){history.replaceState(null,'',location.pathname+location.search);setTimeout(()=>location.reload(),600);}else{fail('Login failed: '+((j&&j.error)||('HTTP '+r.status)));}}).catch(()=>{fail('Network error while sending Telegram login.');});});onReady(function(){const isIndex=(document.body.dataset.page==='index')||!!document.getElementById('save-confirm');if(!isIndex)return;const submitBtn=document.querySelector('.btn-submit');const saveBanner=document.getElementById('save-confirm');if(!submitBtn)return;const escapeHtml=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));document.querySelectorAll('.att-toggle').forEach(chk=>{chk.addEventListener('change',()=>{const id=chk.id.replace('att_','');const cont=document.getElementById('mot_cont_'+id);if(cont)cont.style.display=chk.checked?'none':'block';});});const syncMotTxt=chk=>{const id=chk.id.replace('mot_','');const txt=document.getElementById('mot_text_'+id);if(!txt)return;if(chk.checked)txt.style.display='block';else{txt.style.display='none';txt.value='';}};document.querySelectorAll('.mot-toggle').forEach(chk=>{chk.addEventListener('change',()=>syncMotTxt(chk));syncMotTxt(chk);});submitBtn.addEventListener('click',async()=>{const body=document.body;const dayLabel=body.dataset.dayLabel||'this day';const dateDmy=body.dataset.dateDmy||'';if(!confirm(`Submit attendance for ${dayLabel} (${dateDmy})?`))return;const out={attendance:[]};document.querySelectorAll('.att-toggle').forEach(el=>{if(el.disabled)return;const m=el.id.match(/^att_(\d+)_(\d+)$/);if(!m)return;const sid=+m[1],uid=+m[2];const pres=!!el.checked;const motChk=document.getElementById(`mot_${sid}_${uid}`);const txt=document.getElementById(`mot_text_${sid}_${uid}`);const mot=!pres&&!!(motChk&&motChk.checked);const rea=mot?(txt?.value||''):'';out.attendance.push({schedule_id:sid,user_id:uid,present:pres,motivated:mot,motivation:rea});});const url=`${location.origin}${location.pathname}${location.search}`;try{const resp=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(out)});const text=await resp.text();let j=null;try{const m=text.match(/\{[\s\S]*\}$/);j=m?JSON.parse(m[0]):JSON.parse(text);}catch(_){}if(!j)throw new Error('HTTP '+resp.status);if(!resp.ok||!j.success){alert('Save failed: '+(j.error||('HTTP '+resp.status)));return;}if(saveBanner)saveBanner.style.display='block';const params=new URLSearchParams(location.search);const offset=params.get('offset')||'0';const gid=params.get('group_id');const newBtn=document.createElement('button');newBtn.className='btn-edit';newBtn.type='button';newBtn.textContent='Edit Attendance';newBtn.addEventListener('click',()=>{let href=`edit_attendance.php?offset=${encodeURIComponent(offset)}`;if(gid)href+=`&group_id=${encodeURIComponent(gid)}`;location.href=href;});submitBtn.replaceWith(newBtn);const byName=escapeHtml(String(document.body.dataset.currentUserName||j.marked_by_name||'Unknown'));out.attendance.forEach(r=>{const att=document.getElementById(`att_${r.schedule_id}_${r.user_id}`);if(!att)return;att.checked=!!r.present;att.disabled=true;const cont=document.getElementById(`mot_cont_${r.schedule_id}_${r.user_id}`);if(cont){const td=cont.parentElement;cont.remove();const div=document.createElement('div');div.className='mot-reason';let html='';if(!r.present&&r.motivated&&r.motivation){html+='Reason: '+escapeHtml(r.motivation||'')+'<br>';}html+=`<em>By ${byName}</em>`;div.innerHTML=html;td.appendChild(div);}});document.querySelectorAll('.att-toggle,.mot-toggle,.motiv-text').forEach(el=>{el.disabled=true;});}catch(err){console.error(err);alert('Network error: '+err.message);}});});onReady(function(){if(document.body.dataset.page!=='edit')return;const submitBtn=document.querySelector('.btn-submit');if(!submitBtn)return;document.querySelectorAll('.att-toggle').forEach(chk=>{const apply=()=>{const id=chk.id.replace('att_','');const cont=document.getElementById('mot_cont_'+id);if(cont)cont.style.display=chk.checked?'none':'block';};chk.addEventListener('change',apply);apply();});document.querySelectorAll('.mot-toggle').forEach(motChk=>{const id=motChk.id.replace('mot_','');const txt=document.getElementById('mot_text_'+id);const att=document.getElementById('att_'+id);const apply=()=>{if(motChk.checked){if(txt)txt.style.display='inline-block';if(att){att.checked=false;att.disabled=true;}}else{if(txt){txt.style.display='none';txt.value='';}if(att)att.disabled=false;}};motChk.addEventListener('change',apply);apply();});submitBtn.addEventListener('click',async()=>{const dayLabel=document.body.dataset.dayLabel||'this day';if(!confirm(`Really save edits for ${dayLabel}?`))return;const out={attendance:[]};document.querySelectorAll('.att-toggle').forEach(attEl=>{const m=attEl.id.match(/^att_(\d+)_(\d+)$/);if(!m)return;const sid=+m[1],uid=+m[2];const motEl=document.getElementById(`mot_${sid}_${uid}`);const txtEl=document.getElementById(`mot_text_${sid}_${uid}`);const present=!!attEl.checked;const motivated=!present&&!!(motEl&&motEl.checked);const reason=motivated?(txtEl?.value||''):'';out.attendance.push({schedule_id:sid,user_id:uid,present,motivated,motivation:reason});});const url=`${location.origin}${location.pathname}${location.search}`;try{const resp=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(out)});const j=await resp.json();if(j.success){alert('✅ Changes saved successfully!');location.reload();}else{alert('❌ Save failed: '+(j.error||'unknown'));}}catch(e){alert('❌ Network error: '+e.message);}});});onReady(function(){const primary=document.getElementById('btnPrimary');if(primary){primary.addEventListener('click',()=>{location.href='greeting.php?view=primary';});}const reset=document.getElementById('btnResetView');if(reset){reset.addEventListener('click',()=>{location.href='greeting.php?view=reset';});}});onReady(function(){const sel=document.querySelector('.filters select[name="group_id"]');if(!sel)return;sel.addEventListener('change',()=>{const url=new URL(location.href);url.searchParams.set('group_id',sel.value);location.href=url.toString();});});